{
  "name": "mongo-archiver",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun --watch src/index.ts",
    "prod": "bun run src/index.ts",
    "deploy": "cp .Dockerfile ./dist/ && cp ./captain-definition ./dist/ && tar --strip-components=3 -cvf ./deploy.tar --exclude='*.map' ./dist/* && read -p 'Caprover Istance: ' givenInstance && echo you have entered:  $givenInstance && caprover deploy -t ./deploy.tar -n $givenInstance -a mongo-archiver-api && rm ./deploy.tar"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/pako": "^2.0.0",
    "bun-types": "^0.5.0",
    "@types/node-cron": "^3.0.7"
  },
  "dependencies": {
    "express-validator": "^6.15.0",
    "cors": "^2.8.5",
    "date-fns": "^2.29.3",
    "express": "^4.18.2",
    "jszip": "^3.10.1",
    "mongoose": "^7.0.1",
    "node-cron": "^3.0.2",
    "pako": "^2.1.0",
    "rxjs": "^7.8.0"
  }
}
